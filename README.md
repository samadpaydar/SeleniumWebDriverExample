# SeleniumWebDriverExample
This NetBeans project is a simple example of using **Selenium WebDriver** to write JUnit tests for web application testing.
A sample test is implemented in the test folder.
